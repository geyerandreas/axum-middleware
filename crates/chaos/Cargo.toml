[package]
name = "chaos"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
rand = "0.10.2"
tokio = { version = "1.53.1", features = ["time"] }
tower-layer = "0.3.3"
tower-service = "0.3.3"
